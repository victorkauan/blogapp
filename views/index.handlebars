<div class="bg-light p-5 rounded-lg m-3">
  <h1 class="display-4">Welcome to the Node Blog!</h1>
  <p class="lead">This is a simple BLOG, built with Node.js + Express + MongoDB.</p>
  <hr class="my-4" />
  <p>Here you will be able to read about the news in the computing world.</p>
  <a class="btn btn-primary btn-lg" href="#" role="button">Create an account</a>
</div>

<hr />

<h2>Recent posts:</h2>

{{#each posts}}
  <div class="card mt-4">
    <div class="card-body">
      <h3>{{title}}</h3>
      <p>{{description}}</p>
      <a href="/post/{{slug}}">
        <button class="btn btn-success">Read more</button>
      </a>
      <hr />
      <small>Category: {{category.name}}</small>
      <br />
      <small>Publication date: {{date}}</small>
    </div>
  </div>
{{else}}
  <h4 class="mt-3">No posts!</h4>
{{/each}}